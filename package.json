{
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build && cp dist/index.html dist/404.html || copy dist\\index.html dist\\404.html",
    "deploy": "gh-pages -d dist"
  }
}
